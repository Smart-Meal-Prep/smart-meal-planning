import { React } from 'react';
import endPoints from "../../config/fetch.js";

const Profile = (props) => {
    return(
        <div>
            <h1>Profile</h1>
        </div>
    );
}

export default Profile;