import { React } from 'react';
import endPoints from "../../config/fetch.js";

const Profile = (props) => {
    return(
        <div>
            Profile
        </div>
    );
}

export default Profile;